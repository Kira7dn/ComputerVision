# Integration test handoff

Camera Safety integration tests are under `app/tests/integration`. They test
the package boundary, config profiles, Compose contract, persistence, and API
contracts without importing the legacy runtime.
